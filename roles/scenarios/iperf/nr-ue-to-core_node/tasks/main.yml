---
#
# Deploy iperf servers on core_node with session-based reporting
#
---
- name: Set defaults
  set_fact:
    duration: "{{ duration | default(10) }}"
    sleep: "{{ sleep | default(5) }}"
    core_ip: "{{ hostvars[groups['core_node'][0]].ip }}"

- name: Get all pods in namespace
  kubernetes.core.k8s_info:
    kind: Pod
    namespace: open5gs
  register: all_pods

- name: Filter UE pods by name
  set_fact:
    ue_resources: "{{ all_pods.resources | selectattr('metadata.name', 'search', '^oai-nr-ue') | list }}"

- name: Clean up existing iperf3 clients
  shell: "kubectl exec -n open5gs {{ item.metadata.name }} -- pkill iperf3 || true"
  loop: "{{ ue_resources }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  ignore_errors: yes

- name: Install iperf3 and get IP
  shell:
    cmd: |
      kubectl exec -n open5gs {{ item.metadata.name }} -- bash -c "
      if ! command -v iperf3 >/dev/null 2>&1; then
        apt-get update -qq >/dev/null 2>&1 && apt-get install -y -qq iperf3 iproute2 >/dev/null 2>&1 || true
      fi
      ip -4 addr show oaitun_ue1 2>/dev/null | grep -oP '(?<=inet\s)\d+(\.\d+){3}' | head -n1 | tr -d '\n\r '
      "
  loop: "{{ ue_resources }}"
  loop_control:
    label: "{{ item.metadata.name }}"
  register: ue_ip_results
  changed_when: false

- name: Init UE lists
  set_fact:
    valid_ues: []

- name: Classify valid UEs
  set_fact:
    valid_ues: "{{ valid_ues + [ {'pod': item.item.metadata.name, 'ip': item.stdout | trim} ] }}"
  loop: "{{ ue_ip_results.results }}"
  loop_control:
    label: "{{ item.item.metadata.name }} -> {{ item.stdout | trim }}"
  when:
    - item.stdout is defined
    - item.stdout | trim is search('([0-9]{1,3}\.){3}[0-9]{1,3}')

- name: Pause before tests
  pause:
    seconds: "{{ sleep }}"

- name: Run iperf uplink
  shell: >
    kubectl exec -n open5gs {{ item.pod }} --
    iperf3 -B {{ item.ip }} -c {{ core_ip }} -p {{ (5201 + idx | int) }} -t {{ duration }}
  loop: "{{ valid_ues }}"
  loop_control:
    index_var: idx
    label: "{{ item.pod }} (Port: {{ 5201 + idx | int }})"
  register: uplink_results
  ignore_errors: yes

- name: Debug Uplink Results (Summary)
  debug:
    msg: "Pod {{ item.item.pod }} tested on port {{ 5201 + item.idx | int }}"
  loop: "{{ uplink_results.results }}"
  loop_control:
    label: "{{ item.item.pod }}"
  when: uplink_results.results is defined

- name: Wait for sockets to clear
  pause:
    seconds: 2

- name: Run iperf downlink
  shell: >
    kubectl exec -n open5gs {{ item.pod }} --
    iperf3 -B {{ item.ip }} -c {{ core_ip }} -p {{ (5201 + idx | int) }} -t {{ duration }} -R
  loop: "{{ valid_ues }}"
  loop_control:
    index_var: idx
    label: "{{ item.pod }} (Port: {{ 5201 + idx | int }})"
  ignore_errors: yes
- name: Wait for tests to complete
  pause:
    seconds: "{{ duration | default(10) | int + 5 }}"
  comment: "On attend la fin des tests clients avant de lire les logs"

- name: Get CURRENT session log files
  find:
    paths: /root/IPERF
    patterns: "iperf_*_{{ iperf_timestamp.stdout }}.log"
  register: iperf_files

- name: Read session logs
  command: "cat {{ item.path }}"
  loop: "{{ iperf_files.files | sort(attribute='path') }}"
  loop_control:
    label: "{{ item.path | basename }}"
  register: log_contents
  changed_when: false

- name: "📊 RECAPITULATIF DES TESTS IPERF (Session: {{ iperf_timestamp.stdout }})"
  debug:
    msg: |
      {% for log in log_contents.results %}
      ======================================================================
      FILE: {{ log.item.path | basename }}
      ======================================================================
      {{ log.stdout }}

      {% endfor %}

- name: Verify servers are listening
  shell: netstat -tulpn | grep iperf3
  register: iperf_status
  changed_when: false
  failed_when: false

- name: Debug server status
  debug:
    var: iperf_status.stdout_lines
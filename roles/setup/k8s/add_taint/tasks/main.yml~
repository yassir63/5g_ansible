---
- name: RESTART_K8S_TAINT
  ansible.builtin.meta: noop

- name: Add control-plane taint
  ansible.builtin.shell: |
    kubectl taint nodes --all node-role.kubernetes.io/ran-node:NoSchedule || true
